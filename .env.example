# ── Interakt WhatsApp API ─────────────────────────────────────────────────────
# Sign up free at: interakt.shop (14-day trial, no Meta Business Portfolio needed)
# Get API key from: Dashboard → Settings → Developer → API Key

INTERAKT_API_KEY=your_interakt_api_key_here

# ── (Future) Meta WhatsApp Cloud API ─────────────────────────────────────────
# Switch back to Meta once Meta account restriction is resolved (free, no monthly cost)
# META_PHONE_NUMBER_ID=
# META_ACCESS_TOKEN=
# META_VERIFY_TOKEN=bagwaan_webhook_2024

# ── PostgreSQL ─────────────────────────────────────────────────────────────────
# Local:   postgresql://localhost:5432/hp_apple_advisory
# Render:  postgresql://user:pass@host/dbname  (Render gives you this string)
DATABASE_URL=postgresql://localhost:5432/hp_apple_advisory

# ── Google Gemini (disease detection from images) ────────────────────────────
# Free tier: 1,500 requests/day, no credit card needed
# Get key at: aistudio.google.com → Get API Key (takes 2 minutes)
GOOGLE_GEMINI_API_KEY=your_gemini_api_key

# ── Tomorrow.io (weather) ─────────────────────────────────────────────────────
# Free tier: 500 calls/day — enough for POC
# Sign up at: tomorrow.io
TOMORROW_API_KEY=your_tomorrow_io_api_key

# ── Server ────────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=development
